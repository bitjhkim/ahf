import React from 'react';
import PageTemplate from 'components/common/PageTemplate';
import SchemaContainer from 'containers/schema/SchemaContainer';

const SchemaPage = () => {
  return (
    <PageTemplate>
      <SchemaContainer/>
    </PageTemplate>
  );
};

export default SchemaPage;
